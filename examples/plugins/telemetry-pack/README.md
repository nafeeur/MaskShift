# Telemetry Pack example

Install this trusted local plugin from the MaskShift Pit Garage with the absolute path to this directory, or call:

```text
plugin_install source=/path/to/MaskShift/examples/plugins/telemetry-pack kind=local
```

It registers `race_telemetry_snapshot` and demonstrates event subscriptions and access to runtime managers.
